[**autk-db**](../index.md)

***

[autk-db](../globals.md) / AutkSpatialDb

# Class: AutkSpatialDb

Defined in: [autk-db/src/spatial/SpatialDb.ts:56](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L56)

AutkSpatialDb class provides methods to interact with a DuckDB database for spatial data operations.

It allows loading OSM data, CSV, JSON, custom layers, and grid layers,
as well as performing spatial joins and raw queries.
DuckDB-backed spatial database for OSM, CSV, JSON, and raster data.

Supports multiple isolated workspaces, each with its own schema and tables.

## Example

```ts
const db = new AutkSpatialDb();
await db.init();
const layer = await db.getLayer('osm_buildings');
```

## Constructors

### Constructor

> **new AutkSpatialDb**(): `AutkSpatialDb`

#### Returns

`AutkSpatialDb`

## Accessors

### tables

#### Get Signature

> **get** **tables**(): [`Table`](../type-aliases/Table.md)[]

Defined in: [autk-db/src/spatial/SpatialDb.ts:62](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L62)

##### Returns

[`Table`](../type-aliases/Table.md)[]

## Methods

### buildHeatmap()

> **buildHeatmap**(`params`): `Promise`\<[`Table`](../type-aliases/Table.md)\>

Defined in: [autk-db/src/spatial/SpatialDb.ts:772](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L772)

Builds a heatmap from spatial data by creating a grid and aggregating values.
The heatmap is generated by creating a grid over the bounding box and aggregating values from the source table into each grid cell.

#### Parameters

##### params

[`BuildHeatmapParams`](../interfaces/BuildHeatmapParams.md)

Parameters for building the heatmap, including source table, grid configuration, and aggregation method.

#### Returns

`Promise`\<[`Table`](../type-aliases/Table.md)\>

A promise that resolves to the resulting GridLayerTable containing the heatmap data.

#### Throws

Error if the database or connection is not initialized.

***

### getBoundingBoxFromLayer()

> **getBoundingBoxFromLayer**(`layerName`): `Promise`\<[`BoundingBox`](../interfaces/BoundingBox.md)\>

Defined in: [autk-db/src/spatial/SpatialDb.ts:613](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L613)

Retrieves the bounding box of a layer by its table name.

#### Parameters

##### layerName

`string`

The name of the layer table to retrieve the bounding box from.

#### Returns

`Promise`\<[`BoundingBox`](../interfaces/BoundingBox.md)\>

A promise that resolves to the bounding box of the layer.

#### Throws

Error if the database or connection is not initialized.

#### Throws

Error if the layer table is not found.

#### Throws

Error if the layer table does not have a geometry column.

***

### getCurrentWorkspace()

> **getCurrentWorkspace**(): `string`

Defined in: [autk-db/src/spatial/SpatialDb.ts:181](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L181)

Gets the name of the current active workspace.

#### Returns

`string`

The current workspace name.

***

### getGeoTiffLayer()

> **getGeoTiffLayer**(`tableName`): `Promise`\<`FeatureCollection`\<`null`, `GeoJsonProperties`\>\>

Defined in: [autk-db/src/spatial/SpatialDb.ts:484](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L484)

Retrieves a loaded GeoTIFF table as a FeatureCollection suitable for rendering with autk-map.

The returned collection has a single feature whose `properties.raster` is an array of per-pixel
property objects (one per cell, in row-major top-to-bottom order), plus `rasterResX` / `rasterResY`
dimensions and a `bbox`.

Pass the result directly to `AutkMap.loadRasterCollection()` and supply a `property` callback that
extracts the numeric band value you want to visualise, e.g. `(cell) => cell.band_1 ?? 0`.

#### Parameters

##### tableName

`string`

The name of the GeoTiff table (as given to `loadGeoTiff`).

#### Returns

`Promise`\<`FeatureCollection`\<`null`, `GeoJsonProperties`\>\>

A promise that resolves to a packed raster FeatureCollection.

***

### getLayer()

> **getLayer**(`layerTableName`): `Promise`\<`FeatureCollection`\<`Geometry`, `GeoJsonProperties`\>\>

Defined in: [autk-db/src/spatial/SpatialDb.ts:551](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L551)

Retrieves the GeoJSON representation of a layer by its table name.
The returned FeatureCollection will include a bbox property with the layer's bounding box.

#### Parameters

##### layerTableName

`string`

The name of the layer table to retrieve.

#### Returns

`Promise`\<`FeatureCollection`\<`Geometry`, `GeoJsonProperties`\>\>

A promise that resolves to the GeoJSON FeatureCollection of the layer with bbox.

#### Throws

Error if the database or connection is not initialized.

#### Throws

Error if the layer table is not found or is not a Layer table.

***

### getLayerTables()

> **getLayerTables**(): ([`LayerTable`](../type-aliases/LayerTable.md) \| [`CustomLayerTable`](../type-aliases/CustomLayerTable.md))[]

Defined in: [autk-db/src/spatial/SpatialDb.ts:640](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L640)

Retrieves all layer tables from the loaded tables.

#### Returns

([`LayerTable`](../type-aliases/LayerTable.md) \| [`CustomLayerTable`](../type-aliases/CustomLayerTable.md))[]

An array of `LayerTable` and `CustomLayerTable` objects.

#### Throws

Never throws.

***

### getOsmBoundingBox()

> **getOsmBoundingBox**(): \[`number`, `number`, `number`, `number`\] \| `null`

Defined in: [autk-db/src/spatial/SpatialDb.ts:583](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L583)

Retrieves the OSM bounding box for the current workspace.

#### Returns

\[`number`, `number`, `number`, `number`\] \| `null`

The bounding box as `[minLon, minLat, maxLon, maxLat]`, or `null` if no OSM data has been loaded.

#### Throws

Never throws.

***

### getOsmBoundingBoxWgs84()

> **getOsmBoundingBoxWgs84**(): [`BoundingBox`](../interfaces/BoundingBox.md) \| `null`

Defined in: [autk-db/src/spatial/SpatialDb.ts:601](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L601)

Returns the OSM bounding box in WGS84 (EPSG:4326) for clipping rasters.

#### Returns

[`BoundingBox`](../interfaces/BoundingBox.md) \| `null`

The bounding box or `null` if no OSM data has been loaded.

#### Throws

Never throws.

***

### getTableData()

> **getTableData**(`params`): `Promise`\<[`GetTableDataOutput`](../type-aliases/GetTableDataOutput.md)\>

Defined in: [autk-db/src/spatial/SpatialDb.ts:658](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L658)

Retrieves the data from any table as an array of plain JavaScript objects.
This method works with all table types (CSV, JSON, Layer, Grid, etc.).

#### Parameters

##### params

[`GetTableDataParams`](../interfaces/GetTableDataParams.md)

Parameters including table name and optional pagination (limit, offset).

#### Returns

`Promise`\<[`GetTableDataOutput`](../type-aliases/GetTableDataOutput.md)\>

A promise that resolves to an array of objects representing the table rows.

#### Throws

Error if the database or connection is not initialized.

#### Throws

Error if the table is not found.

***

### getWorkspaces()

> **getWorkspaces**(): `string`[]

Defined in: [autk-db/src/spatial/SpatialDb.ts:173](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L173)

Gets the list of all available workspaces.

#### Returns

`string`[]

An array of workspace names.

***

### init()

> **init**(): `Promise`\<`void`\>

Defined in: [autk-db/src/spatial/SpatialDb.ts:109](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L109)

Initializes DuckDB, loads the spatial extension, and creates use-case instances.

#### Returns

`Promise`\<`void`\>

A promise that resolves when initialization is complete.

#### Throws

If DuckDB WebAssembly fails to load or the spatial extension cannot be installed.

#### Example

```ts
await db.init();
```

***

### loadCsv()

> **loadCsv**(`params`): `Promise`\<[`CsvTable`](../type-aliases/CsvTable.md)\>

Defined in: [autk-db/src/spatial/SpatialDb.ts:348](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L348)

Loads a CSV file into the database and returns the created CsvTable.

#### Parameters

##### params

[`LoadCsvParams`](../interfaces/LoadCsvParams.md)

Parameters for loading the CSV file, including file path and table name.

#### Returns

`Promise`\<[`CsvTable`](../type-aliases/CsvTable.md)\>

A promise that resolves to the created CsvTable.

#### Throws

Error if the database or connection is not initialized.

***

### loadCustomLayer()

> **loadCustomLayer**(`params`): `Promise`\<[`CustomLayerTable`](../type-aliases/CustomLayerTable.md)\>

Defined in: [autk-db/src/spatial/SpatialDb.ts:403](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L403)

Loads a custom layer from a GeoJSON file and returns the created CustomLayerTable.
If OSM bounding box is available, it will be automatically applied to crop the layer.

#### Parameters

##### params

[`LoadCustomLayerParams`](../interfaces/LoadCustomLayerParams.md)

Parameters for loading the custom layer, including file path, table name, and layer type.

#### Returns

`Promise`\<[`CustomLayerTable`](../type-aliases/CustomLayerTable.md)\>

A promise that resolves to the created CustomLayerTable.

#### Throws

Error if the database or connection is not initialized.

***

### loadGeoTiff()

> **loadGeoTiff**(`params`): `Promise`\<[`GeoTiffTable`](../type-aliases/GeoTiffTable.md)\>

Defined in: [autk-db/src/spatial/SpatialDb.ts:458](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L458)

Loads a GeoTIFF raster file into the database.
Uses DuckDB's spatial extension (GDAL-backed ST_Read) to parse the file.
The resulting table has a `geometry` column (cell centroids) and a
`properties` struct column containing one field per raster band.

#### Parameters

##### params

[`LoadGeoTiffParams`](../interfaces/LoadGeoTiffParams.md)

Parameters including the file URL or ArrayBuffer, output table name,
  and optional coordinate transformation settings.

#### Returns

`Promise`\<[`GeoTiffTable`](../type-aliases/GeoTiffTable.md)\>

A promise that resolves to the created GeoTiffTable.

***

### loadGridLayer()

> **loadGridLayer**(`params`): `Promise`\<[`GridLayerTable`](../type-aliases/GridLayerTable.md)\>

Defined in: [autk-db/src/spatial/SpatialDb.ts:434](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L434)

Loads a grid layer and returns the created GridLayerTable.
If no bounding box is provided in params, the OSM bounding box will be used if available.

#### Parameters

##### params

[`LoadGridLayerParams`](../interfaces/LoadGridLayerParams.md)

Parameters for loading the grid layer, including grid size, cell size, and optional bounding box.

#### Returns

`Promise`\<[`GridLayerTable`](../type-aliases/GridLayerTable.md)\>

A promise that resolves to the created GridLayerTable.

#### Throws

Error if the database or connection is not initialized.

***

### loadJson()

> **loadJson**(`params`): `Promise`\<[`JsonTable`](../type-aliases/JsonTable.md)\>

Defined in: [autk-db/src/spatial/SpatialDb.ts:364](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L364)

Loads a JSON file into the database and returns the created JsonTable.

#### Parameters

##### params

[`LoadJsonParams`](../interfaces/LoadJsonParams.md)

Parameters for loading the JSON file, including file path and table name.

#### Returns

`Promise`\<[`JsonTable`](../type-aliases/JsonTable.md)\>

A promise that resolves to the created JsonTable.

#### Throws

Error if the database or connection is not initialized.

***

### loadLayer()

> **loadLayer**(`params`): `Promise`\<[`LayerTable`](../type-aliases/LayerTable.md)\>

Defined in: [autk-db/src/spatial/SpatialDb.ts:381](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L381)

Loads a layer from an OSM input table and returns the created LayerTable.

#### Parameters

##### params

[`LoadLayerParams`](../interfaces/LoadLayerParams.md)

Parameters for loading the layer.

#### Returns

`Promise`\<[`LayerTable`](../type-aliases/LayerTable.md)\>

A promise that resolves to the created LayerTable.

#### Throws

Error if the database or connection is not initialized.

#### Throws

Error if the OSM input table is not found or is not of the correct type.

***

### loadOsm()

> **loadOsm**(`params`): `Promise`\<[`OsmLoadTimings`](../interfaces/OsmLoadTimings.md)\>

Defined in: [autk-db/src/spatial/SpatialDb.ts:240](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L240)

Loads OSM data from the Overpass API and optionally loads layers based on the provided parameters.
When autoLoadLayers is enabled, this method will automatically extract and process specific layers
(e.g., buildings, roads, surface) from the OSM data, and optionally polygonize the surface layer.

#### Parameters

##### params

[`LoadOsmParams`](../type-aliases/LoadOsmParams.md)

Parameters for loading OSM data and layers.

#### Returns

`Promise`\<[`OsmLoadTimings`](../interfaces/OsmLoadTimings.md)\>

A promise that resolves when the OSM data and layers are fully loaded.

#### Throws

Error if the database or connection is not initialized.

***

### rawQuery()

> **rawQuery**\<`T`\>(`params`): `Promise`\<[`Table`](../type-aliases/Table.md) \| `T`\>

Defined in: [autk-db/src/spatial/SpatialDb.ts:735](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L735)

Executes a raw SQL query and returns the result.

#### Type Parameters

##### T

`T` = [`RawQueryOutput`](../type-aliases/RawQueryOutput.md)

#### Parameters

##### params

[`RawQueryParams`](../interfaces/RawQueryParams.md)

Parameters for the raw query, including the SQL query string and output type.

#### Returns

`Promise`\<[`Table`](../type-aliases/Table.md) \| `T`\>

A promise that resolves to a Table if output type is 'CREATE_TABLE', otherwise returns the query result of type T.

#### Throws

Error if the database or connection is not initialized.

***

### removeLayer()

> **removeLayer**(`tableName`): `Promise`\<`void`\>

Defined in: [autk-db/src/spatial/SpatialDb.ts:755](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L755)

Drops a table from the database and removes it from the current workspace.

#### Parameters

##### tableName

`string`

The name of the table to remove.

#### Returns

`Promise`\<`void`\>

A promise that resolves when the table has been dropped.

#### Throws

Error if the database or connection is not initialized.

***

### setWorkspace()

> **setWorkspace**(`name`): `Promise`\<`void`\>

Defined in: [autk-db/src/spatial/SpatialDb.ts:153](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L153)

Switches to a workspace, creating it if it doesn't exist.

#### Parameters

##### name

`string`

The name of the workspace to switch to.

#### Returns

`Promise`\<`void`\>

A promise that resolves when the workspace is set.

#### Throws

If the database has not been initialized.

***

### spatialQuery()

> **spatialQuery**(`params`): `Promise`\<[`Table`](../type-aliases/Table.md)\>

Defined in: [autk-db/src/spatial/SpatialDb.ts:717](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L717)

Performs a spatial join between two tables and returns the resulting table.
The method can either create a new table or update an existing one based on the parameters.

#### Parameters

##### params

[`SpatialQueryParams`](../interfaces/SpatialQueryParams.md)

Parameters for the spatial join operation, including source and target tables, join type, and output table name.

#### Returns

`Promise`\<[`Table`](../type-aliases/Table.md)\>

A promise that resolves to the resulting table after the spatial join.

#### Throws

Error if the database or connection is not initialized.

***

### updateTable()

> **updateTable**(`params`): `Promise`\<[`Table`](../type-aliases/Table.md)\>

Defined in: [autk-db/src/spatial/SpatialDb.ts:686](https://github.com/urban-toolkit/autark/blob/be27d66c55f885979ab5d4dff54048f4e2c468a1/autk-db/src/spatial/SpatialDb.ts#L686)

Updates an existing table with new data.

For layer tables (OSM, GeoJSON), the input data should be a GeoJSON FeatureCollection.
For non-layer tables (CSV, JSON), the input data should be an array of objects.

#### Parameters

##### params

`Omit`\<[`UpdateTableParams`](../interfaces/UpdateTableParams.md), `"workspace"`\>

Parameters for updating the table:
  - tableName: The name of the table to update
  - data: The new data (FeatureCollection for layers, Record<string, unknown>[] for CSV/JSON)
  - strategy: 'replace' (drop and recreate) or 'update' (update existing records by ID)
  - idColumn: Required for 'update' strategy. Supports 'id' or 'properties.attribute_name' format

#### Returns

`Promise`\<[`Table`](../type-aliases/Table.md)\>

A promise that resolves to the updated Table with refreshed column metadata.

#### Throws

Error if the database or connection is not initialized.

#### Throws

Error if the table is not found.

#### Throws

Error if idColumn is not provided when using 'update' strategy.
